org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Force Gradle to use JDK 17 which is compatible with the Android Gradle/Kotlin toolchain
# Previously we set a hard-coded Java home which is invalid on GitHub runners.
# Remove or comment this so the CI workflow's Java setup (actions/setup-java)
# can provide a proper JAVA_HOME. If you need to force a local path while
# developing on a specific machine, uncomment and adjust the line below.
# org.gradle.java.home=/usr/lib/jvm/java-17-openjdk
